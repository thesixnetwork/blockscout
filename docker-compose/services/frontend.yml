services:
  frontend:
    # image: node:22.14.0-bullseye
    # entrypoint: []
    # restart: no
    image: frontend-custom:latest
    pull_policy: never
    restart: always
    container_name: 'frontend'
    ports:
      - "3000:3000"
    env_file:
      -  ../envs/common-frontend.env
    environment:
      - NODE_ENV=development
    #   - CHOKIDAR_USEPOLLING=true
    #   - WATCHPACK_POLLING=true
    #   - NEXT_PUBLIC_IS_DEVELOPMENT=true
    # volumes:
    #   # Mount local frontend source for hot reload
    #   - /Users/sankanvicha/Desktop/project/sixscan-evm/blockscout-frontend:/app
    #   # - frontend_node_modules:/app/node_modules
    #   - /app/node_modules # เปลี่ยนเป็นแบบ anonymous volume แทน
    #   - /app/.next # เพิ่มอันนี้เพื่อ cache build file
    # working_dir: /app
    # command: sh -lc "yarn install --ignore-engines && yarn dev -p 3000"
